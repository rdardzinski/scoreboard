package org.sports.mapper;

import org.sports.model.ScoreEntry;
import sun.reflect.generics.reflectiveObjects.NotImplementedException;

public class ScoreEntryMapper {

    /**
     * Parses a match string and map it to a ScoreEntry object.
     *
     * @param input The match string in the format "teamA homeScore - teamB awayScore".
     * @return ScoreEntry representing the match.
     * @throws IllegalArgumentException if the input format is invalid.
     */
    public static ScoreEntry mapFromString(String input) {
        throw new NotImplementedException();
    }
}

